# Veloura Client

Next.js storefront and admin interface for Veloura Beauty.

```bash
cp .env.example .env.local
npm ci
npm run dev
```

See the repository root README for environment variables, database setup,
verification commands, and deployment instructions.
